.DS_Store
Thumbs.db
*.log
node_modules/

# --- Добавлено 22.08.2026, после аудита репозиториев ---
# Этот репозиторий (lampardi05-docs) ПУБЛИЧНЫЙ, а лежит он в корне рабочей
# папки проекта. Любой `git add -A` отсюда затягивает всё подряд — 20.08
# так и случилось, откатывали `git reset --mixed HEAD~1`. Проверено:
# в историю секреты не попали ни разу. Чтобы не полагаться на удачу,
# закрываем всё, что появилось в папке после перезапуска проекта.

# Секреты проекта — никогда и никуда
_access.local.md
*.local.md

# Код сайта живёт в собственном репозитории site/
site/

# Передача между сессиями: внутренние заметки и выгрузки заказчика
_claude-transfer/

# Рабочие документы перезапущенного проекта
lampardi-*.md
questions-for-gamza.md
_cleanup.md
figma-export/
_archive-bitrix/

# Остальное, что появилось в папке после перезапуска
Frame 27.svg
arrow-right.svg
deploy-preview.sh
lampardi-stack.html
lampardi_docs/
.playwright-mcp/

# Сценарий промо-ролика — внутренний документ
promokod-video-scenariy.md
